<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Required to load ebooks -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- Dar de baja el permiso de identificador del anunciante -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" /> <!-- MapBox permissions -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/kitty"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kitty"
        android:supportsRtl="true"
        android:theme="@style/Theme.KittyReader.SplashScreen"
        tools:targetApi="31">
        <activity
            android:name=".domain.controllers.StoragePermissionsActivity"
            android:exported="false" />
        <activity
            android:name=".views.MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.AppCompat.NoActionBar" />


        <activity
            android:name=".domain.controllers.firebase.UploadActivity"
            android:exported="false" />
        <activity
            android:name=".views.EpubActivity"
            android:exported="false"
            android:label="EpubReader"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".domain.controllers.auth.AuthActivity"
            android:exported="false" />




        <activity
            android:name=".views.PdfActivity"
            android:exported="true"
            android:label="KittyReader"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".views.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KittyReader.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.folioreader.ui.activity.SearchActivity"
            android:exported="true">

            <!-- Otras configuraciones de actividad -->
        </activity> <!-- Agregar un provider para el manejo seguro de datos entre apps -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.cutedomain.kittyreader"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>